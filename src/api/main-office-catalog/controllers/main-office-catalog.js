'use strict';

/**
 * main-office-catalog controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::main-office-catalog.main-office-catalog');
